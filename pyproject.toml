[project]
name = "djquickstart"
version = "0.2.1"
description = "CLI tool for fast Django project setup"
authors = [{name="emiskas"}]
dependencies = ["click", "Django"]

[project.scripts]
djquickstart = "djquickstart.cli:cli"
